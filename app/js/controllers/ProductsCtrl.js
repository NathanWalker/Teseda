AppControllers.controller("ProductsCtrl", [
  "LogService", "$scope", "$rootScope", "$location", "$http", function(log, s, $rootScope, $location, $http) {
    return log('ProductsCtrl init()');
  }
]);
