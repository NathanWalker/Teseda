'use strict';

describe('router.js -', function(){

  describe('router test:', function(){

    it('check variables', function(){
      expect(ROUTER.when).to.exist;
      expect(ROUTER.alias).to.exist;
      expect(ROUTER.otherwise).to.exist;
      expect(ROUTER.replaceUrlParams).to.exist;
      expect(ROUTER.routeDefined).to.exist;
      expect(ROUTER.getRoute).to.exist;
      expect(ROUTER.routePath).to.exist;
      expect(ROUTER.install).to.exist;
    });

    it('check functionality', function(){
//      expect(ROUTER.when).to.exist;
//      expect(ROUTER.alias).to.exist;
//      expect(ROUTER.otherwise).to.exist;
//      expect(ROUTER.replaceUrlParams).to.exist;
//      expect(ROUTER.routeDefined).to.exist;
//      expect(ROUTER.getRoute).to.exist;
//      expect(ROUTER.routePath).to.exist;
//      expect(ROUTER.install).to.exist;
    });

  });
});