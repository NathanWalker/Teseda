sudo npm cache clean
bower cache clean
sudo npm install -g foreman
sudo npm install -g grunt-cli
sudo npm install -g karma
sudo npm install -g bower
sudo npm install
bower install

echo "--------------------------"
echo "Done. You should be all set now."
